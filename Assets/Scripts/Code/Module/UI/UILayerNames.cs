namespace TaoTie
{
    public enum UILayerNames : byte
    {
        GameBackgroundLayer,
        BackgroundLayer,
        GameLayer,
        SceneLayer,
        NormalLayer,
        TipLayer,
        TopLayer,
    }
}